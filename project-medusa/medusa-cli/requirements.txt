# MEDUSA CLI - Python Dependencies
# Version: 1.0.0

# CLI Framework
# Note: typer 0.20.0+ requires click 8.0.0+ for compatibility
typer[all]>=0.20.0
click>=8.0.0

# Terminal UI
rich>=12.0.0,<14.0.0
prompt-toolkit>=3.0.0

# HTTP Client (async)
httpx==0.26.0

# Configuration Management
pyyaml==6.0.1

# Agent Framework
langgraph>=0.0.10
langchain>=0.1.0
langchain-core>=0.1.0
langgraph-checkpoint-postgres>=1.0.0  # For native persistence

# LLM Integration - Local inference via Ollama (default)
# No cloud dependencies required for local-first architecture
# Optional cloud packages can be installed separately:
# - pip install openai  (for OpenAI/GPT support)
# - pip install anthropic  (for Anthropic/Claude support)

# Report Generation
jinja2==3.1.3

# Web API Server - FastAPI (async-native for LangGraph)
fastapi>=0.104.0      # Modern async web framework
uvicorn[standard]>=0.24.0  # ASGI server with WebSocket support
python-multipart>=0.0.6  # For file uploads and form data

# Database (for operation logging and checkpointing)
psycopg>=3.1.0  # PostgreSQL adapter for LangGraph checkpointer
psycopg-pool>=3.1.0  # Connection pooling

# Neo4j Graph Database (for World Model)
neo4j==5.14.1
pydantic>=2.9.0  # Updated for Python 3.13 compatibility

# Testing
pytest==7.4.3
pytest-cov==4.1.0

# Code quality
black==24.1.1
flake8==7.0.0
mypy==1.8.0

# Utilities
python-dotenv==1.0.0
boto3>=1.34.0
botocore>=1.34.0
aiohttp>=3.9.0
